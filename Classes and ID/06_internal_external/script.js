function message() {
  // Defines a function named "message"
  alert("BETTER TO TRY SOMEWHERE!"); // Displays an alert with the message "BETTER TO TRY SOMEWHERE!"
}

function internal() {
  // Defines a function named "internal"
  alert(
    "Order is in progress waiting for the information from the account teams"
  ); // Displays an alert with the message "Order is in progress waiting for the information from the account teams"
}
